<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vjti.fests" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:label="@string/register_user"
            android:screenOrientation="portrait" />

        <!-- Enthusia Activities -->
        <activity
            android:label="@string/enthusia_fest_name"
            android:name=".enthusia.EnthusiaStartActivity"
            android:screenOrientation="portrait" />

        <activity
            android:label="@string/enthusia_events"
            android:name=".enthusia.EnthusiaEventsActivity"
            android:screenOrientation="portrait" />

        <!-- Pratibimb Activities -->
        <activity
            android:label="@string/pratibimb_fest_name"
            android:name=".pratibimb.PratibimbStartActivity"
            android:screenOrientation="portrait" />

        <!-- Technovanza Activities -->
        <activity
            android:label="@string/technovanza_fest_name"
            android:name=".technovanza.TechnovanzaStartActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>
