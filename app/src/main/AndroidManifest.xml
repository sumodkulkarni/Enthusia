<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vjti.fests" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Enthusia Activities -->
        <activity android:name=".enthusia.EnthusiaStartActivity" />

        <!-- Pratibimb Activities -->
        <activity android:name=".pratibimb.PratibimbStartActivity" />

        <!-- Technovanza Activities -->
        <activity android:name=".technovanza.TechnovanzaStartActivity" />

    </application>

</manifest>
